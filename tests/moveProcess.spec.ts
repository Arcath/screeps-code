const MoveProcess = require('../lib/processTypes/creepActions/move').MoveProcess
//import {expect} from 'chai'

const gl: any = global

gl._ = require('lodash')

describe('Move Process', function(){
  it('should generate the correct path', /* function(){
    var proc = new MoveProcess({
      name: 'test-move-process',
      creep: 'test'
    })

    proc.run()
  }*/)
})
